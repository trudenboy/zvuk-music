query getPlaylists($ids: [ID!]!) {
  getPlaylists(ids: $ids) {
    id
    title
    isPublic
    description
    duration
    image {
      src
    }
    tracks {
      id
    }
    userId
    isDeleted
    shared
    branded
    updated
    searchTitle
  }
}
